# Tag2 Clone High-Fidelity

## Install
1. Install Node.js
2. Run `npm install`

## Start Server
Run `npm start` and open `http://localhost:3000`

## Gameplay
- Arrow keys to move
- Red player is the tagger
- Multiplayer enabled via Socket.io